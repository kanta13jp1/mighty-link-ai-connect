@AGENTS.md

## Claude Code

- Use this file as the project memory entry point for VSCode + Claude Code.
- Keep persistent shared rules in `AGENTS.md`; add Claude-specific review or documentation guidance below only when it should be loaded every Claude Code session.
- Treat `.claude/settings.local.json` and `CLAUDE.local.md` as local-only settings. Do not commit them.
- Prefer documentation, risk triage, QA maintenance, and review tasks unless the user explicitly asks Claude Code to implement code.
