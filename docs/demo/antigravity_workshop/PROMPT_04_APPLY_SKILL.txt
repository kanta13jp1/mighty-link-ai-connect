Prompt 4 / /frontend-designで残りのテストをGREENにする

/frontend-design

@SITE_BRIEF.md、@TEST_SPEC.md、@tests/test_site_contract.py を読み、初版を改善してください。テスト2ファイルは変更しません。

実装すること:
- 情報の優先順位、文字、余白、配色を整え、研修投影でも読める画面にする
- 4フィルター「すべて／開発／ナレッジワーク／計画・自動化」を追加し、件数を5・4・1・5にする
- 各カードに比較ボタンを付け最大2製品まで選べるようにする。3件目は追加せず案内する
- 選択件数、製品名、向いている仕事をaria-liveへ表示する
- app.jsでaria-pressed、キーボード操作、上限2件に対応する
- 色は#00A5E3、#EF7E00、緑、白、濃い文字色を使う

守る境界:
- 5製品と説明を維持し、SteeringとPowersはKiro固有機能として扱う
- 外部依存、送信、永続保存、commit、pushは行わない
- 変更はindex.html、styles.css、app.jsだけ。製品追加、料金、クォータ、ランキング、診断、検索、export、テーマ切替、動画、画像生成、Visual Feedback模擬は追加しない
- 開始前に変更予定3ファイルを報告し、範囲外が必要なら停止する

検証すること:
- python -m unittest discover -s tests -vでT01-T08がすべてPASS。テストを削除、skip、弱体化してPASSさせない
- 1440x900と390x844で横スクロール、文字切れ、重なりが0

最後に「Skillが変えた判断／REDからGREENになったテスト／ブラウザ検証」を3行で報告してください。
