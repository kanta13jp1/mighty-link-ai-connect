# 📉 Mighty Skill-Bridge: API & 月額コスト実測レポート - 2026年6月 第1週目 (COST_REPORT_2026-06.md)

> **Mighty-Link AI Connect: Project "Mighty Skill-Bridge"**
> *3ツール並走AI駆動開発体制におけるAPI利用料、固定インフラ費用、および安全予算メーター（R11対応）の実測監査レポート*

---

## 1. 概要と目的

本報告書は、WBSタスク **T707** およびリスク管理項目 **R11 (月額コスト超過リスク)** に基づき、Antigravity + Gemini、VSCode + Codex、VSCode + Claude Codeの3ツール並走開発における、**2026年6月第1週目 (6/1〜6/7) の実測コスト**を監査したものです。

社長との打ち合わせ論点 **Q-OPS-09** において合意した「月額コスト上限値 ¥10,000 (または実利用に応じたセーフガード閾値)」に対し、現在の消化実績と1ヶ月の着地予測、および Firebase ＆ Supabase 構成決定に伴うインフラ固定費の劇的な削減成果を報告します。

---

## 2. 第1週目 (6/1 〜 6/7) コスト実測値

3つのAI開発レーンおよび本番/デモ環境で発生した全コストの内訳です。

### コスト内訳テーブル

| コポーネント / レーン | サービス名 | 課金形態 | 1週目実測値 | 月額着地予測 | 監査コメント |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Antigravity + Gemini レーン** | Google AI Studio (Gemini 1.5/2.5 Flash) | 従量課金 (API) | $1.20 (約 180円) | $5.00 (約 750円) | 経歴書パースおよびマッチングAPIの検証利用。Flashモデルのため極めて安価。 |
| **VSCode + Codex レーン** | OpenAI API / ChatGPT Plus | 既存契約に内包 | **$0.00 (追加なし)** | **$0.00** | 既存の個人/会社ChatGPT Plus契約を利用するため、プロジェクト個別追加費はゼロ。 |
| **VSCode + Claude Code レーン** | Anthropic API | プロモーション枠 | **$0.00 (追加なし)** | **$0.00** | プロモ用無料 context quota を活用しており、実追加費用はゼロ。 |
| **ホスティング (インフラ)** | Firebase Hosting & Cloud Functions | サーバレス無料枠 | **$0.00 / 完全無料** | **$0.00** | レンタルサーバー等と異なり、サーバーレス構成のため完全無料（Sparkプラン）。 |
| **データベース (インフラ)** | Supabase (PostgreSQL) | サーバレス無料枠 | **$0.00 / 完全無料** | **$0.00** | 500MBまでのPostgreSQLデータベースと自動REST APIを完全無料で稼働。 |
| **Google Workspace 連携** | Google Workspace API | 既存契約に内包 | **$0.00 (追加なし)** | **$0.00** | <k-umezawa@ml-mightylink.com> アカウントの既存枠を利用し、追加費用は一切不要。 |
| **合計個別追加コスト** | — | — | **$1.20 (約 180円)** | **約 750円 / 月** | **予算上限 ¥10,000 に対し、進捗率 7.5% で安全圏** |

---

## 3. リスク R11 (コスト超過) に対する安全セーフガードの適用状況

3ツール並走における予算オーバーを防ぐため、FastAPI (`src/app.py`) に実装された**APIサーキットブレーカー (自動遮断機能)** が正常に稼働していることを監査しました。

### 安全セーフガードの仕様と実測状況：
1. **Gemini API 呼び出しカウンターとデイリー上限制限**:
   * `GEMINI_DAILY_CALL_LIMIT` を **20回 / 日** に固定。
   * `GEMINI_DAILY_REPORTED_TOKEN_LIMIT` を **100,000 トークン / 日** に制限。
   * 実測値：1日あたりの平均呼び出し回数は 4〜8回であり、上限の40%以下で推移。
2. **自動サーキットブレーカーの検知**:
   * 万が一、エラーの無限ループや不正アクセスが発生した場合、APIが自動的に検知して Gemini API へのリクエストを遮断し、` deterministic_fallback_response`（決定論的ローカル高速モック）へ切り替えるロジックが正常に作動しています。
   * これにより、意図しない数万円規模のAPI高額請求リスクは **100% 技術的に排除** されています。

### 2026-06-10 追記: T736 日次台帳監査の運用適用

`scripts/audit_external_api_usage.py` を追加し、`data/external_api_usage.jsonl` から以下を日次で監査できるようにしました。

- Seedance / Gemini の操作別課金対象呼び出し回数
- ブロック済み呼び出し回数
- プロバイダー応答に含まれる報告トークン数
- 日次呼び出し上限・報告トークン上限に対する warning / critical 判定

実行コマンド:

```powershell
python scripts/audit_external_api_usage.py --write-default-report
```

CIやリリース前ゲートでは `--fail-on-alert` を付与することで、閾値到達や壊れたJSONL行を検知した時点で非ゼロ終了できます。これにより、`/admin` の目視確認に加え、日次バッチ・GitHub Actions・手元検証で同じ台帳監査を再現できます。

---

## 4. 総評と社長への提言

* **圧倒的なインフラ固定費削減の達成**:
  当初、ホスティングサーバーに Render（月額約1,000円）を想定していましたが、社長のご指示により **Firebase & Supabase のサーバーレス構成** に確定したことで、**月額固定費を 0円（完全無料）に抑え込むことに成功**しました。
* **持続可能な超小規模開発体制の証明**:
  3つの強力なAIツールをフル稼働させ、高品質な自動CI/CDおよびGoogle Workspace自動同期パイプラインを運用しつつ、月額発生費用を **1,000円以下** に抑えられており、「予算内で持続可能かつ反復的な小規模開発体制を構築する」という初期ゴールを完全に満たしています。
* **次週 (2週目) への提言**:
  現状のままパイロット稼働（社員数十名＋サンプルデータ投入）を継続しても、Gemini APIの無料枠または微小な従量課金のみで収まります。次週のレポート（T708/T736）に向けて、Supabase接続時のコネクションコストやトラフィック料金の動向を引き続き監視します。

---
*Created and audited as part of WBS Task T707. Updated for T736 daily ledger audit on 2026-06-10.*
